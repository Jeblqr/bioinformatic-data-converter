% Generated by roxygen2: do not edit by hand
\name{convert_file}
\alias{convert_file}
\title{Convert a bioinformatics data file}
\usage{
convert_file(
  input_file,
  output_file,
  auto_suggest = TRUE,
  column_mapping = NULL,
  keep_unmatched = FALSE,
  verbose = TRUE,
  generate_report = TRUE,
  report_dir = NULL
)
}
\arguments{
\item{input_file}{Character. Path to input file}

\item{output_file}{Character. Path to output file}

\item{auto_suggest}{Logical. Use automatic column mapping suggestions (default: TRUE)}

\item{column_mapping}{Named list. Manual column mapping (original_name = "standard_name")}

\item{keep_unmatched}{Logical. Keep columns that don't match standard patterns (default: FALSE)}

\item{verbose}{Logical. Print detailed information (default: TRUE)}

\item{generate_report}{Logical. Generate conversion report (default: TRUE)}

\item{report_dir}{Character. Directory for saving reports (default: same as output)}
}
\value{
A tibble containing the converted data
}
\description{
Convert a bioinformatics data file from its original format to a standardized format.
Automatically detects file format and omics type.
}
\examples{
\dontrun{
# Convert with auto-suggestion
result <- convert_file(
  input_file = "gwas_data.tsv",
  output_file = "standardized_gwas.tsv"
)

# Convert with manual mapping
result <- convert_file(
  input_file = "data.txt",
  output_file = "output.tsv",
  column_mapping = list(CHR = "chr", POS = "pos", P = "pval")
)
}
}
